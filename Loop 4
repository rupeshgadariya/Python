#            for loop
# 1. Write a python script to print each character of a string with its 
#    corresponding Unicode.
for s in input("Enter a string : "):
    print(ord(s), end=' ')
 
# 2. Write a Python script to print only vowels of the given string
for s in input("Enter a string : "):
    if s in "aeiouAEIOU":
        print(s,end='')

# 3. Write a Python script to count occurrence of spaces in a given string.
count = 0
for s in input("Enter a string : "):
    if s in ' ':
        count += 1
print("Occurrence of spaces in a given string is ",count)

# 4. Write a Python script to print unique digits of a given integer.
temp = ''
for s in input("Enter a number "):
    if s not in temp:
        print(s, end= ' ')
        temp += s

# 5. Write a Python script to count number of digits in a given number
count = 0
for s in input("Enter a number "):
    count += 1
print("Count number of digits ", count)
